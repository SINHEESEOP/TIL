# TIL 저장소 작성 가이드

이 저장소는 개인 학습 내용을 정리하는 TIL(Today I Learned) 모음이다. 새 문서를 쓰거나 기존 문서를 고칠 때는 아래 기준을 따른다.

## 문체

- 이모지는 쓰지 않는다. 제목, 소제목, 본문, 표 어디에도 넣지 않는다. 표에서 여부를 표시할 때도 체크마크 이모지(✅❌⚠️) 대신 "예", "아니오", "조건부" 같은 말로 쓴다.
- AI가 쓴 티가 나는 표현을 피한다.
  - "~라고 할 수 있습니다", "결론적으로", "요약하자면" 같은 상투적 연결어를 줄인다.
  - 모든 문서 끝에 습관적으로 "정리" 섹션을 붙이지 않는다. 위에서 이미 표나 소제목으로 정리가 됐다면 마지막에 같은 내용을 다시 나열할 필요는 없다.
  - "정말 중요한", "반드시 알아야 할" 같은 강조어를 근거 없이 반복하지 않는다.
  - 문장으로 풀어써도 되는 내용까지 억지로 번호 매기기/불릿으로 쪼개지 않는다.
- 실제로 노트에 적어두듯 담백하고 직접적으로 쓴다.

## 강조 문법 검증

`**굵게**`, `*기울임*`을 쓸 때는 실제로 렌더링되는지 반드시 확인한다.

- 마커 안쪽에 공백을 넣지 않는다. `** 이렇게 **`가 아니라 `**이렇게**`.
- 수식이나 코드처럼 보이는 표현에 `*`가 들어가면(`a * a = a` 등) 강조 문법과 충돌할 수 있으니 백틱으로 감싼다.
- 다 쓴 뒤에는 마크다운 프리뷰로 실제 렌더링 결과를 한 번 확인한다.

## GitHub 알림 블록

`[!NOTE]`, `[!TIP]`, `[!IMPORTANT]`, `[!WARNING]`, `[!CAUTION]`은 내용 성격에 실제로 맞을 때만 쓴다. 문서마다 억지로 하나씩 채우지 않는다.

- NOTE: 본문 흐름과는 별개로 알아두면 좋은 부가 정보
- TIP: 실무에서 바로 써먹을 수 있는 요령이나 권장 방식
- IMPORTANT: 이 개념을 제대로 이해하려면 반드시 짚어야 할 핵심, 특히 흔히 하는 오해
- WARNING: 잘못 적용하면 버그나 장애로 이어지는 지점
- CAUTION: 되돌리기 어렵거나 위험한 조작

## 예시

이해를 돕는 예시는 넣되 실제로 있을 법한 상황만 쓴다. 개념 설명을 위해 억지로 지어낸 시나리오는 넣지 않는다.

## 제목

문서 제목(H1)은 한국어로 자연스럽게 쓴다. 굳이 언더스코어나 하이픈으로 단어를 이어 붙일 필요는 없다. 영어 표기가 더 직관적인 용어(HTTP, API, JVM처럼 업계에서 원어 그대로 쓰는 고유명사)는 그대로 영어를 쓰고, 그 외에는 한국어를 우선한다.

파일명은 기존 저장소 관례(영문 소문자와 언더스코어)를 그대로 따른다. 제목과 파일명은 별개 문제다 — 파일명이 영문이어도 본문 제목은 한국어로 쓴다.

## 디렉토리 분류

새 문서를 쓰기 전에 어느 디렉토리에 들어가야 하는지 먼저 판단한다.

- 맞는 디렉토리가 있으면 그 안에 넣는다.
- 마땅한 디렉토리가 없으면 새로 만들되, 기존 구조를 따른다. 이 저장소는 문서를 추가하면 해당 디렉토리의 README, 상위 카테고리 README, 루트 README.md 세 곳의 목차를 같이 갱신하는 방식으로 되어 있다. 새 카테고리를 만들 때도 이 3단 연결을 유지한다.
- 지금 구조보다 더 나은 배치가 보이면 그냥 넣지 말고 리팩토링을 먼저 제안한다.

## 여러 영역에 걸치는 개념

하나의 개념이 두 카테고리 이상에서 다뤄질 수 있을 때는 문서를 복제하지 않는다.

- 개념이 가장 근본적으로 설명되는 카테고리에 원본 문서를 둔다.
- 다른 카테고리에서 같은 개념이 필요하면 그 카테고리의 README나 관련 문서에서 원본으로 링크만 건다.
- 카테고리마다 실제로 다루는 관점이 다르다면(같은 멱등성이라도 HTTP 관점과 메시지 큐 재처리 관점은 설명할 내용이 다르다) 각 카테고리에 관점이 다른 문서를 따로 쓰되, 개념의 정의 자체는 원본 문서 하나에서만 설명하고 나머지 문서는 그쪽을 참조하게 한다.
